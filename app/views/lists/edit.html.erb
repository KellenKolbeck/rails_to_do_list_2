<h1>Edit list</h1>

<!-- <% if @list.errors.any? %>
  <h3> PLease fix these errors:</h3>
  <ul>
    <% @list.erros.full_messages.each do |message| %>
    <%end%>
  </ul>
<% end %>

<%= form_for @list do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %> -->
<%= render "form" %>
